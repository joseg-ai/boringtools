{
  "name": "@domos/diagnostics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --import tsx --watch src/server.ts",
    "check": "tsc --project tsconfig.json",
    "build": "node scripts/build.mjs",
    "start": "node --env-file-if-exists=.env dist/server.mjs"
  },
  "dependencies": {
    "@domos/contracts": "0.1.0",
    "@fastify/cors": "11.3.0",
    "@fastify/rate-limit": "11.2.0",
    "dns-packet": "5.6.1",
    "fastify": "5.12.1",
    "ipaddr.js": "2.5.0",
    "tldts": "7.4.11",
    "zod": "4.5.4"
  },
  "devDependencies": { "@types/dns-packet": "5.6.5" }
}
