import type { ToolId } from '@domos/catalog';
export interface Guide {
  intro: string;
  steps: readonly string[];
  example: string;
  exampleNote: string;
  interpretation: string;
  pitfalls: readonly string[];
  limits: string;
}
export const GUIDES = {
  'ipv4-subnet-planner': {
    intro: 'Address planning is easier when every allocation remains part of a visible whole. Start from one IPv4 block, split where you need a smaller network, and keep the rest available without creating gaps.',
    steps: ['Enter a base CIDR and create the plan. The engine normalizes or rejects the network according to the allocation rules.', 'Select one allocation in the proportional map or keyboard table. Split creates two equal child networks. Add a note and color to explain its purpose.', 'Select two sibling leaves to join them. Review the annotation first: the displayed note and color become the joined block annotation.', 'Prepare a JSON export for a round trip, or CSV for a spreadsheet. Download is a separate explicit action.'],
    example: 'Base: 10.42.8.0/24\nSplit into: 10.42.8.0/25 + 10.42.8.128/25\nSplit the first child again for two /26 allocations.',
    exampleNote: 'This private-address example illustrates subdivision; it does not recommend an address plan for your environment.',
    interpretation: 'The diagram uses total address counts, not usable-host counts. A /31 point-to-point block has two usable addresses; a /32 represents one host. /0 spans the entire IPv4 address space. The table shows network, usable ranges and the appropriate broadcast semantics.',
    pitfalls: ['Equal prefix length does not make two blocks siblings. An invalid join is rejected.', 'Annotations do not reserve addresses in DHCP, cloud networks or IPAM. This is an offline plan.', 'An imported plan must exactly partition its base with canonical, disjoint leaves. The last valid plan remains visible after a rejected import.'],
    limits: 'Up to 4,096 leaves. JSON imports are bounded to 2 MiB. JSON is the supported import format; CSV is export-only in this release. No automatic saving.',
  },
  'dns-explorer': {
    intro: 'A DNS lookup is a time-bound observation from a resolver, not a universal view of a domain. Inspect the record types relevant to your investigation and keep negative answers separate from resolver failures.',
    steps: ['Enter a public DNS name. For reverse lookups, choose PTR and provide an appropriate IP or reverse name.', 'Select one or more of A, AAAA, MX, CNAME, NS, TXT, SOA and PTR.', 'Submit the query explicitly. Review each record type independently, including its TTL and response state.'],
    example: 'Name: example.com\nTypes: A, AAAA, MX\nTTL is shown in seconds for every returned record.',
    exampleNote: 'The example only fills an input. Real records are retrieved when you submit and may differ over time.',
    interpretation: 'NXDOMAIN means the queried name does not exist according to the resolver. NODATA means there is no answer for that record type. Neither should be mistaken for a timeout. MX preferences are priorities, not delivery-speed measurements; smaller values are generally preferred.',
    pitfalls: ['A cached answer can differ from an authoritative server or another region.', 'TXT records can consist of multiple chunks; the readable value joins those chunks.', 'A CNAME or missing AAAA record alone does not establish service availability.'],
    limits: 'Eight supported record types, bounded query budgets and an 8-second API deadline. Targets are sent to the diagnostics API and resolver. Partial reports retain their stopping reason.',
  },
  'email-dns-policy': {
    intro: 'Published email policies describe intentions and keys. They cannot tell you whether a particular message passed authentication. Use this tool to inspect SPF dependencies, DMARC policy and one known DKIM selector without promising deliverability.',
    steps: ['Enter the email domain, not an entire email address.', 'If you have a DKIM-Signature header, copy its s= selector into the optional selector field. Leave it empty rather than guessing.', 'Submit and review the independent SPF, DMARC and DKIM status sections. Read all limitations and unresolved dependencies.'],
    example: 'Domain: example.com\nDKIM selector: selector1 (only if known)\nDKIM owner: selector1._domainkey.example.com',
    exampleNote: 'This shows how a selector names a DNS key location; it does not assert that this example publishes that key.',
    interpretation: 'Present, absent, invalid and indeterminate have different meanings. DMARC organizational-domain fallback is shown explicitly. SPF lookup count is the exploration budget consumed, not proof that a sender evaluation satisfies the RFC ten-term limit.',
    pitfalls: ['A present DKIM key is not a verified signature.', 'SPF macros and unvisited dependencies can prevent a complete static exploration.', 'A strict DMARC policy does not prove every receiving system enforces it.'],
    limits: 'DNS records only, with a 32-query budget, bounded dependency depth and a 10-second API deadline. No selector enumeration or sender authentication.',
  },
  'http-inspector': {
    intro: 'Redirect and header investigations often need less than a browser. A bounded server-side inspection can show response codes and header pairs without rendering remote content or running its scripts.',
    steps: ['Enter a public HTTP or HTTPS URL without credentials or confidential query parameters.', 'Keep HEAD unless you specifically need GET behavior. GET is explicit and can have side effects on a poorly designed target.', 'Submit, then inspect the hop table and individual response headers. Check the termination reason before treating the chain as complete.'],
    example: 'URL: https://example.com\nMethod: HEAD\nRead: status code, Location, Cache-Control, Content-Type',
    exampleNote: 'The target is contacted by the diagnostics service only after submission, never opened automatically in your browser.',
    interpretation: 'Each hop preserves its URL, method, remote address, elapsed time and duplicate header pairs. A 301 or 302 describes a redirect; the following hop is a separate observation. A timeout, blocked destination or loop may yield a partial report.',
    pitfalls: ['HEAD and GET can produce different headers. This tool does not silently switch methods.', 'A response header is not a complete security assessment.', 'Private destinations and unsafe redirect targets are blocked; this is not a tool for reaching your internal network.'],
    limits: 'Up to five redirects and six hops, bounded headers and a 12-second API deadline. Remote response bodies are not displayed. No cookies or custom credentials are forwarded.',
  },
  'email-header-analyzer': {
    intro: 'Message headers can explain the route an email claims to have taken. Their usefulness depends on which systems you trust; a pasted header is evidence to inspect, not an authenticated statement.',
    steps: ['Open the message source in your mail client and copy the header block, excluding the message body.', 'Paste into the analyzer and run it locally.', 'Review unfolded fields, Received hops, parsed times and reported authentication assertions.'],
    example: 'Subject: Build notification\nFrom: notifications@example.com\nAuthentication-Results: mx.example.net; spf=pass\nReceived: from relay.example.com by mx.example.net;',
    exampleNote: 'These deliberately incomplete sample headers demonstrate structure, not a verified route.',
    interpretation: 'Header unfolding joins continuation lines into one field. Repeated fields remain visible. Routing delays depend on parseable timestamps and system clocks; missing or odd times deserve investigation rather than automatic blame.',
    pitfalls: ['Authentication-Results and Received-SPF values are reported, not independently verified.', 'A sender can prepend untrusted headers. Decide where your trusted receiving infrastructure begins.', 'Header contents can identify people, systems or internal addresses. Downloading or copying moves that information outside the workspace.'],
    limits: 'Up to 1 MiB of input, 2,000 header fields and 1,000 routing or authentication entries under the shared output budget. No network lookup or signature verification.',
  },
  chmod: {
    intro: 'Unix permission notation compresses several independent choices. Translate octal values, toggle owner/group/other rights or apply an explicit symbolic change before using a command in your terminal.',
    steps: ['Choose octal mode, permission checkboxes or symbolic changes.', 'For symbolic changes, provide the starting mode and explicit u, g, o or a targets.', 'Review both octal and symbolic results, especially special bits. Copy the command only after checking the file context.'],
    example: '0644 = rw-r--r--\nu+x,g-w applied to 0644 gives owner execute permission\nr = 4, w = 2, x = 1',
    exampleNote: 'The workspace computes the exact result. No command is executed and no file permissions are changed.',
    interpretation: 'The fourth octal digit represents setuid, setgid and sticky bits. Uppercase S or T indicates a special bit without the corresponding execute bit. Directory execute permission means traversal, not running a directory.',
    pitfalls: ['Permissions interact with ownership, ACLs and mount options beyond this calculator.', 'Do not make a file world-writable simply to remove an access error.', 'Conditional X and permission-class copying are outside this release; use explicit rwxst changes.'],
    limits: 'Three- or four-digit octal input. Symbolic expressions require explicit targets and +, - or =. This is a local calculator, not a shell.',
  },
  'password-generator': {
    intro: 'A useful secret must come from an unpredictable source and fit the system where you use it. The generator uses browser cryptographic randomness and exposes the selection rules rather than hiding them behind a strength meter.',
    steps: ['Choose a password or passphrase. Select length or word count and the number to generate.', 'For passwords, select character classes and whether every selected class is required. For phrases, choose a separator and optional capitalization.', 'Generate explicitly. Copy only the chosen secret into a password manager or intended destination; nothing is copied automatically.'],
    example: 'Password: 20 characters, all four classes, count 1\nPassphrase: 6 independently selected words, hyphen separator\nNo sample secret is recommended for real use.',
    exampleNote: 'Changing case or separators for a phrase is a formatting choice, not extra random word selection.',
    interpretation: 'Entropy is labeled exact or lower-bound according to the generation rules. The selection pool and passphrase wordlist are reported. This estimate describes generation, not protection against phishing, reuse or compromise of a service.',
    pitfalls: ['Do not reuse a secret across accounts.', 'Clipboard managers and synchronized clipboards can retain copied secrets.', 'Avoid generating credentials while screensharing. Resetting the workspace does not clear external clipboards or screenshots.'],
    limits: '4-256 password characters, 4-24 phrase words and up to 20 outputs per run. Local-only, no automatic history. Generated values stay in memory until cleared or navigated away.',
  },
  'json-yaml-workbench': {
    intro: 'Formatting should make a document easier to understand without quietly changing what it means. Choose the source format explicitly, then validate, format, minify JSON or convert between JSON and YAML.',
    steps: ['Select JSON or YAML as the source and paste the document.', 'Choose an action. Conversion requires an explicit target; minify is only available for JSON.', 'Review output notices alongside the result. A valid document is not necessarily appropriate for the system that will consume it.'],
    example: '{\n  "service": "edge",\n  "replicas": 3,\n  "enabled": true\n}',
    exampleNote: 'Try formatting with two or four spaces, then converting to YAML. Compare the resulting scalar types as well as the appearance.',
    interpretation: 'Validation returns a validity result without a transformed document. Formatting and conversion report whether text changed and how comments were handled. Numeric values need special care because ordinary JavaScript numbers cannot exactly represent every JSON integer.',
    pitfalls: ['YAML comments have no direct JSON equivalent and may be removed during conversion.', 'Unsupported or ambiguous structures should produce a clear error, not a silently rounded or coerced document.', 'Syntax validation is not schema validation for your application.'],
    limits: '1 MiB input, 2 MiB serialized output, depth 64, 50,000 nodes and bounded aliases. No document is uploaded or used as executable HTML.',
  },
  base64: {
    intro: 'Base64 translates bytes into a restricted text alphabet. It is useful for transport, but it does not protect a secret. Decide what the raw bytes mean before encoding or decoding them.',
    steps: ['Choose encode or decode and select standard or URL-safe alphabet.', 'Choose UTF-8 for text or hex for explicit byte values. That selection describes the raw side of the conversion.', 'Choose padding behavior for encoding, then run and inspect the byte count.'],
    example: 'UTF-8 input: hello\nStandard Base64: aGVsbG8=\nHex for the same bytes: 68656c6c6f',
    exampleNote: 'The example illustrates the difference between bytes and the textual representation of those bytes.',
    interpretation: 'Standard Base64 uses + and /; the URL-safe alphabet uses - and _. Padding affects the encoded representation, not the underlying payload. Decoding supports valid padded or unpadded input and reports malformed data rather than inventing a result.',
    pitfalls: ['Encoding does not mean encrypting; anyone with the value can decode it.', 'Arbitrary binary bytes may not form valid UTF-8. Choose hex output when inspecting binary content.', 'Do not assume all decoders accept mixed alphabets or malformed padding.'],
    limits: '1 MiB input under a 2 MiB output envelope. UTF-8 and hex are explicit; there is no automatic detection or input history.',
  },
  'url-workbench': {
    intro: 'A URL is a structured value, not just a string to split on punctuation. Inspect its components or construct ordered query parameters without visiting the target.',
    steps: ['Choose parse, build, encode or decode.', 'For build, enter an absolute base URL and add name/value pairs in their intended order. Decide whether to replace the existing query.', 'For encoded text, choose URI-component or form rules. Review the resulting text and parsed fields.'],
    example: 'https://example.com/search?tag=network&tag=security\nTwo tag parameters remain two ordered pairs.\nForm spaces: +    URI-component spaces: %20',
    exampleNote: 'The example is displayed as text in the tool and is never automatically opened.',
    interpretation: 'Duplicate query parameter names can be meaningful. The table preserves those pairs rather than collapsing them into one object property. A URL parser also distinguishes path, query, fragment, hostname and credentials.',
    pitfalls: ['A plus sign has form-encoding semantics; it is not universally a space.', 'A fragment is not the same thing as a query parameter.', 'URLs may contain tokens or credentials. This local tool does not fetch them, but copying or downloading can expose them elsewhere.'],
    limits: 'Absolute URLs up to 16,384 characters and up to 1,000 ordered parameters. General encode/decode text is bounded to 1 MiB. No navigation or remote requests.',
  },
  'jwt-decoder': {
    intro: 'Decoding a JSON Web Token is useful for inspection, but it is not signature verification. Treat the header and payload as untrusted statements even when the structure is valid and the timestamps appear reasonable.',
    steps: ['Paste a three-part compact token.', 'Optionally set an explicit reference instant with an ISO offset; otherwise the current instant is used.', 'Decode and inspect the header, payload and exp, nbf and iat claims under the persistent UNVERIFIED label.'],
    example: 'header.payload.signature\nexp = expiration time\nnbf = not-before time\niat = issued-at time',
    exampleNote: 'These names describe conventional claims. Their presence does not establish authenticity or authorization.',
    interpretation: 'Timestamp statuses compare recognized claim values with the reference instant. They do not validate the issuer, audience, allowed algorithms or signing key. A future expiration cannot rescue a forged token.',
    pitfalls: ['Never grant access based on this decoder output.', 'An algorithm named in a token header is untrusted until your verifier applies its own allowlist.', 'A real bearer token is a credential. Avoid screenshots, public paste services and synchronized clipboards.'],
    limits: 'Local decoding of three-part tokens, up to the shared 1 MiB input limit. No key retrieval, signature check, issuer lookup or network request. Verification always remains unperformed.',
  },
  'sha-checksums': {
    intro: 'A SHA checksum provides a compact fingerprint of bytes. Compare against a trusted expected value to detect changes, while remembering that a matching hash does not itself identify the publisher.',
    steps: ['Choose text or a local file and select SHA-256, SHA-384 or SHA-512.', 'For text, explicitly choose UTF-8 or hex. For files, select a file within the 250 MiB limit.', 'Run, watch progress and cancel if needed. Copy the exact digest for comparison.'],
    example: 'Same text with a trailing newline = different bytes = different digest\nSHA-256 output: 64 hexadecimal characters\nSHA-512 output: 128 hexadecimal characters',
    exampleNote: 'The workspace reports byte length to help catch an unexpected newline, encoding or file choice.',
    interpretation: 'File hashing reads bounded chunks in a worker, keeping the UI responsive and avoiding a full buffered upload. The selected algorithms operate on the same input bytes. Cancellation terminates the worker and produces no accepted checksum for that run.',
    pitfalls: ['Get the expected checksum from a trusted source; a compromised download page could replace both file and digest.', 'Text encoding and whitespace affect the digest.', 'These SHA functions are not a password-storage scheme.'],
    limits: 'Files up to 250 MiB with 1 MiB chunks and a 120-second worker deadline. Text up to 1 MiB. Files never leave the browser through this tool.',
  },
  'regex-tester': {
    intro: 'Regular expressions are compact programs whose runtime depends on the pattern and input together. Test matches in a terminable worker, inspect captures, and keep execution limits visible.',
    steps: ['Enter a JavaScript regex pattern without surrounding slash delimiters.', 'Choose flags and paste test input. Set the maximum match count if you need fewer results.', 'Run and inspect match positions, numbered captures and named groups. A deadline failure is a real failure, not a claim that nothing matched.'],
    example: 'Pattern: (?<service>[a-z]+)-(\\d+)\nFlags: g\nInput: edge-12 api-34\nNamed capture: service',
    exampleNote: 'Indices follow JavaScript string semantics. A zero-width match may have empty text and still be a valid match.',
    interpretation: 'The supported flags are d, g, i, m, s, u, v and y where the browser supports them. Flags must be unique; u and v cannot be combined. The engine marks match-count truncation explicitly.',
    pitfalls: ['Nested backtracking patterns can consume the deadline with surprisingly short input.', 'JavaScript regex syntax may differ from server-side regex dialects.', 'This release inspects matches only. A replacement operation is not present in the frozen execution contract.'],
    limits: '8 KiB pattern, 200 KiB input, up to 1,000 matches and a 1-second worker deadline. Cancel terminates the worker; no remote execution.',
  },
  'text-diff': {
    intro: 'A useful diff makes changes legible before you decide what to keep. Compare two versions by line for structural edits or by word for prose-level changes, without interpreting either side as HTML.',
    steps: ['Paste the original version into Before and the revised version into After.', 'Select line or word mode. Choose whether whitespace differences matter.', 'Compare, inspect added and removed sections, and explicitly copy or download the unified diff if needed.'],
    example: 'Before: deploy edge --replicas 2\nAfter:  deploy edge --replicas 3\nWord mode highlights the changed argument.',
    exampleNote: 'The original inputs stay side by side on wide screens and stack on smaller screens.',
    interpretation: 'Added and removed content has textual markers as well as color. Statistics describe the selected comparison units. Unified context lines control the surrounding content in exported changes, not an automatic merge.',
    pitfalls: ['Ignoring whitespace may conceal important changes in whitespace-sensitive languages.', 'A diff is not a merge or syntax validator.', 'HTML-like input is shown literally. It is never rendered as a page or executable markup.'],
    limits: '512 KiB per side, bounded diff parts and a 2-second worker deadline. Large or expensive comparisons fail visibly instead of freezing the workspace.',
  },
  'cron-helper': {
    intro: 'A cron expression only becomes a schedule when you pair it with a timezone and a parser convention. Inspect ten upcoming instants before using an expression in the scheduler that actually runs your jobs.',
    steps: ['Enter exactly five fields: minute, hour, day of month, month and day of week.', 'Set an IANA timezone and optionally a reference ISO instant with an offset.', 'Preview ten runs and compare their local displays, ISO instants and UTC offsets.'],
    example: '*/15 9-17 * * MON-FRI\nEvery fifteen minutes during hours 9 through 17 on weekdays\nTimezone: America/New_York',
    exampleNote: 'An hour range includes the whole final hour. This expression can include 17:45, not just 17:00.',
    interpretation: 'Day-of-month and day-of-week constraints use OR matching. The pinned cron-parser library determines DST behavior; review runs around clock changes if timing matters. The output is a preview, not a background scheduler.',
    pitfalls: ['Six-field expressions with seconds are not accepted.', 'Extensions such as L, W, #, ? and @ aliases are outside this release.', 'A cloud service may use a different parser or timezone default. Compare against that service before deploying a schedule.'],
    limits: 'Standard lists, ranges, steps and month/day names only. Exactly ten upcoming runs are requested by the UI. No job is created or executed.',
  },
  'epoch-time': {
    intro: 'Epoch mistakes often come from guessing the unit. Choose seconds or milliseconds explicitly, and distinguish the instant from the timezone used to display it.',
    steps: ['Choose epoch-to-date or ISO-to-epoch.', 'Select the epoch unit. ISO input must include Z or an explicit offset.', 'Set the display timezone, convert, and compare seconds, milliseconds, ISO and local representations.'],
    example: '1704067200 seconds\n1704067200000 milliseconds\n2024-01-01T00:00:00Z\nThese describe the same instant.',
    exampleNote: 'A different display timezone changes the local clock reading, not the underlying instant.',
    interpretation: 'Fractional seconds can carry millisecond precision. Milliseconds must be integers. Negative epoch values describe instants before the epoch. The output includes the timezone offset to make the local representation unambiguous.',
    pitfalls: ['A large integer is not evidence of a particular unit; the tool never guesses.', 'A naive local datetime without an offset can be ambiguous near DST transitions and is not accepted.', 'More than three fractional second digits would imply precision this tool does not preserve.'],
    limits: 'ISO instants require offsets and at most millisecond precision. Timezones must be supported IANA identifiers. Local-only; the current-time shortcut runs only when clicked.',
  },
} satisfies Record<ToolId, Guide>;
